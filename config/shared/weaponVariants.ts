/** Name suffixes that mark a weapon as a variant of a base weapon. */
export const VARIANT_SUFFIXES = [" Prime", " Wraith", " Vandal"];

/** Name prefixes that mark a weapon as a variant of a base weapon. "Dex " is
 *  absent on purpose: WFM lists dex_nikana and dex_dakra as riven families of
 *  their own, so folding it asks for a weapon that was never made. */
export const VARIANT_PREFIXES = ["MK1-", "Kuva ", "Tenet ", "Prisma "];
